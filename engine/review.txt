No Temporal Stability (Still Missing)

Your PRD mentions it—but your code doesn’t implement it.

Result:
Violations flicker frame-to-frame
UI will rapidly switch sync/desync
Fix (mandatory):

Inside SocialDistancingLogic:

Maintain last N frames
Only confirm violation if persistent

Right now, your system is visually unstable.

3. Your Distance Logic Is Too Naive

Current:

Raw pixel distance
Problem:

This breaks badly with:

Perspective
Camera angle
Example:

Two people far away → flagged incorrectly

Fix (minimal, not overkill):

Normalize distance using body scale:

e.g., divide by torso height or hip width

Without this, your demo will look inconsistent.

4. Identity Is Not Stable

You use:

index-based IDs (0,1,2…)
Problem:

Between frames:

Person 0 ≠ same person
Result:
UI labels jump
Logs become meaningless
Fix:

Add lightweight matching:

Match new midpoints to previous frame (nearest neighbor)

You don’t need tracking—just consistency.

5. You’re Overloading the Backend Loop

Inside generate_frames():

You are doing:

inference
logic
logging
encoding
API state updates

All in one loop

Problem:

This becomes:

hard to debug
hard to scale
potential bottleneck
Fix:

At minimum:

Separate:
processing
logging
streaming
6. UI Scaling Assumption Is Fragile

Frontend assumes:

const scaleX = canvas.width / 640;
const scaleY = canvas.height / 480;

Problem:
Hardcoded resolution = wrong overlays on different cameras
Fix:

Send actual frame dimensions via API.

7. Violation Data Structure Is Inconsistent

Backend:

(i, j, dist)

Frontend:

expects (i, j)
You’re ignoring distance in UI
Missed opportunity:

You could:

scale intensity by distance
show severity
8. Action Detection Is Overreach (Right Now)

Your detect_action() is:

heuristic-heavy
noisy
not validated
Problem:
Adds noise to system
Not part of core objective
Recommendation:

Either:

Remove it
or
Isolate it completely (don’t mix with distancing logic)